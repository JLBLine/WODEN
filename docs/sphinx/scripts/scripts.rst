``WODEN`` Scripts
++++++++++++++++++++
See below information about scripts that are installed with ``WODEN``. Note all scripts are installed from the ``scripts`` directory in the ``WODEN`` repository. The are more scripts in that directory than listed below, but only the ones listed below are tested; feel free to use any others, but beware their functionality cannot be guaranteed.

.. toctree::
  :maxdepth: 1

  run_woden
  add_instrumental_effects_woden
  add_woden_uvfits
  concat_woden_uvfits
  uv2ms